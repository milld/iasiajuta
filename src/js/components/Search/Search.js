import React, { Component, PropTypes } from 'react';

class Search extends Component {
  render() {
    return null;
  };
}

Search.defaultProps = {
};

Search.PropTypes = {
};

export default Search;