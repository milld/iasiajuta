import React, { Component, PropTypes } from 'react';

class Footer extends Component {
  render() {
    return null;
  };
}

Footer.defaultProps = {
};

Footer.PropTypes = {
};

export default Footer;